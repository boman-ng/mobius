interface:
  display_name: "Mobius Loop"
  short_description: "Run a locked Mobius Objective through the full loop."
  default_prompt: "Run the full locked Mobius Objective until accepted, blocked, or a real stop gate."

dependencies:
  tools:
    - type: "mcp"
      value: "mobius-review"
      description: "Mobius Review recorded judgment MCP server"
      transport: "stdio"
