interface:
  display_name: "Mobius Loop"
  short_description: "Run an explicit Mobius Objective from live state"
  default_prompt: "Use $mobius-loop to run or continue the explicitly named Mobius Objective from live Core state."
dependencies:
  tools:
    - type: "mcp"
      value: "mobius"
      description: "Project-local Mobius Core MCP server"
      transport: "stdio"
policy:
  allow_implicit_invocation: false
