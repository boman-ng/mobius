interface:
  display_name: "Mobius Loop"
  short_description: "Run a locked Mobius goal through the full plan loop."
  default_prompt: "Run the full locked Mobius plan loop until accepted, terminal blocked, or a real stop gate."

dependencies:
  tools:
    - type: "mcp"
      value: "mobius-cv"
      description: "MobiusCV recorded review MCP server"
      transport: "stdio"
