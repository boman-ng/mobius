interface:
  display_name: "Mobius Copilot"
  short_description: "Manage an explicit Mobius Objective contract"
  default_prompt: "Use $mobius-copilot to activate, revise, abandon, or continue Map installation for the explicitly named Mobius Objective."
dependencies:
  tools:
    - type: "mcp"
      value: "mobius"
      description: "Project-local Mobius Core MCP server"
      transport: "stdio"
policy:
  allow_implicit_invocation: false
