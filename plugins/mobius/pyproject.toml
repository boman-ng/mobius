[project]
name = "mobius-plugin"
version = "0.5.0"
description = "Mobius Codex plugin helpers and Review MCP server."
requires-python = ">=3.11"
dependencies = [
  "mcp>=1,<2"
]

[tool.uv]
package = false
