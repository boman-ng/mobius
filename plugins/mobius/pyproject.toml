[project]
name = "mobius-plugin"
version = "0.2.0"
description = "Mobius Codex plugin helpers and MobiusCV MCP server."
requires-python = ">=3.11"
dependencies = [
  "mcp>=1,<2"
]

[tool.uv]
package = false
